#저자 코드 가져옴, 필요시 없애기
spring:
  sql:
    init:
      mode: never
#여긴 내가 추가함 db 연결하려고
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/urecabook
    username: ureca
    password: ureca